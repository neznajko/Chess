////////////////////////////////////////////////////////////////
# ifndef COM_HPP
# define COM_HPP
////////////////////////////////////////////////////////////////
# include "node.hpp"
////////////////////////////////////////////////////////////////
class Com {
private:
    Node*       node;
    std::string nput;
    //_
    bool Fetch() { return static_cast<bool>( std::cin >> nput ); }
    Move GetMov();
    void undo();
public:
    Com( Node* node_ ): node( node_ ){}
    bool exec();
};
////////////////////////////////////////////////////////////////
# endif// COM_HPP
////////////////////////////////////////////////////////////////
//
//
